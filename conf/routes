# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /                    controllers.Application.index
# User pages
GET        /user/*user          controllers.Application.user(user)
#Submit pages
GET        /submit/*user        controllers.Application.submit(user)
# Login page
GET        /login               controllers.Auth.login

POST       /submitLogin         controllers.Auth.submitLogin
# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file        controllers.Assets.at(path="/public", file)

GET        /api/auth            controllers.Api.auth
POST       /api/submitPrompt    controllers.Api.submitPrompt
    